{{ form_start(form) }}
    <div class="flex flex-col">
        {{ form_label(form.title) }}
        {{ form_widget(form.title, {'attr': {
            'class': 'mt-4 w-full  md:w-1/3 rounded-md border border-gray-300 px-2 py-2 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50',
        }}) }}
        {{ form_errors(form.title) }}
    </div>

    <div class="mt-8">
        {{ form_label(form.categories) }}
        {{ form_widget(form.categories, {'attr': {
            'class': 'selectcategory hidden mt-4 w-full rounded-md border border-gray-300 px-2 py-2 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50',
        }}) }}
    </div>

{#    <div class="my-8">#}
{#        <multiple-select items='{{ categories|json_encode }}' edit-mode="0" #}{#selected-items="{{ selectedCategories|json_encode }}" update-route="{{ path('app_article_edit_categories', {'id': article.id}) }}"#}{#></multiple-select>#}
{#    </div>#}

    <div class="mt-8">
        {{ form_label(form.headingContent) }}
        {{ form_widget(form.headingContent, {'attr': {
            'class': 'mt-4 w-full rounded-md border border-gray-300 px-2 py-2 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50',
            'rows': '4',
            'cols': '50'
        }}) }}
        {{ form_errors(form.headingContent) }}
    </div>

    <div class="mt-8">
        {{ form_label(form.content) }}
        {{ form_widget(form.content) }}
        {{ form_errors(form.content) }}
    </div>

    <div class="mt-4 flex gap-2 justify-end">
        <button class="rounded bg-slate-50 px-4 py-2 font-medium transition hover:bg-slate-600 hover:text-white text-sm">{{ button_label|default('Save article') }}</button>
    </div>
{{ form_end(form) }}


